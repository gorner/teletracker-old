= form_for [@series, @episode] do |f|
  - if @episode.errors.any?
    #error_explanation
      %h2= "#{pluralize(@episode.errors.count, "error")} prohibited this episode from being saved:"
      %ul
        - @episode.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :watched
    = f.check_box :watched
  .actions
    = f.submit 'Save'
